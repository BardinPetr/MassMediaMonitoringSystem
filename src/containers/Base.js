import React, {Component} from "react";

class CustomLayout extends Component {
    render() {
        return (
            <div>
                <h1 style={{color: "#FFF"}}>My React App!</h1>
            </div>
        );
    }
}

export default CustomLayout;